# 一键提报 - 不提报商品与手动提报 PRD

## 1. 文档信息

- 文档版本：V1.0
- 创建日期：2026-05-25
- 适用页面：团长系统 / 合作管理 / 一键提报
- 原型文件：`index.html`
- 页面地址：`/Users/kongqing/Documents/优化一件提报/index.html`

## 2. 页面结构
### 2.1 不提报商品抽屉

抽屉分为两个功能区：

- 排除规则区：配置新增规则、导入规则。
- 已添加的不提报商品区：查看、筛选、删除、一键提报。

## 3. 不提报商品配置

### 3.1 排除类型

支持三种排除类型：

| 类型 | 配置字段 | 规则说明 |
| --- | --- | --- |
| 按活动+商品排除 | 活动ID、商品ID | 只排除指定活动下的指定商品 |
| 按商品排除 | 商品ID | 该商品将在所有活动中被排除 |
| 按活动排除 | 活动ID | 该活动下的全部商品都会被排除 |

### 3.2 表单交互

- 用户切换排除类型后，表单字段随类型变化。
- 当前类型下仅展示所需字段。
- 规则说明文案随类型和输入内容实时变化。
- 说明前置图标使用圆圈感叹号样式。

### 3.3 添加按钮状态

- 当前配置字段全部为空时，“添加”按钮置灰不可点击。
- 当前配置字段有输入时，“添加”按钮可点击。
- 商品 ID 不为空但长度不是 19 位数字时，允许点击，但点击后吐司提示：`商品ID输入错误!`
- 商品 ID 为空且当前类型需要商品 ID 时，点击后吐司提示：`商品ID不能为空!`
- 活动 ID 为空且当前类型需要活动 ID 时，点击后吐司提示：`活动ID不能为空!`

### 3.4 商品 ID 校验

- 商品 ID 必须为 19 位数字。
- 校验触发时机：点击“添加”按钮。
- 校验失败仅吐司提示，不新增数据。

## 4. 导入排除商品

### 4.1 入口

在“不提报商品”抽屉的排除规则区点击“导入”按钮，打开“导入排除商品”弹窗。

### 4.2 弹窗内容

弹窗包含：

- 标题：导入排除商品。
- 关闭按钮。
- 模板下载入口。
- 上传区域。
- 取消按钮。
- 确认导入按钮。

说明文案：

`点击 下载 Excel 模板 后，填入期望排除商品，再上传至此，即可排除指定部分商品的提报。单次最多支持导入 3000 条商品ID`

### 4.3 上传规则

- 支持点击上传。
- 支持拖拽上传。
- 支持 `.xlsx`、`.xls` 格式。
- 单次最多支持导入 3000 条商品 ID。
- 超过限制时展示错误提示。

## 5. 已添加的不提报商品列表

### 5.1 筛选

支持两个筛选项：

- 商品ID
- 活动ID

筛选项横向展示，包含：

- 商品ID标签 + 输入框
- 活动ID标签 + 输入框
- 重置按钮

### 5.2 商品列表

列表标题：商品列表。

标题右侧展示操作区：

- 一键提报按钮。
- 查看手动提报记录文字链接。

操作顺序：

`一键提报` → `查看手动提报记录`

### 5.3 表格字段

| 字段 | 说明 |
| --- | --- |
| 商品ID | 仅展示商品 ID；无商品 ID 时展示 `-` |
| 活动ID | 仅展示活动 ID；无活动 ID 时展示 `-` |
| 添加时间 | 展示该条数据添加时间 |
| 操作 | 删除 |

### 5.4 展示规则

- 表格不展示商品名称、商品图片、活动名称等扩展信息。
- 表头字号为 14px。
- 删除按钮字号为 14px。
- 商品列表标题字号为 16px。
- 有数据时不展示缺省态。
- 无数据时展示缺省态：暂无内容。
- 列表底部展示分页器。

### 5.5 分页规则

- 每页 10 条。
- 展示总条数。
- 支持上一页、下一页。
- 当前页高亮。
- 分页器展示在缺省态下方。

## 6. 手动提报

### 6.1 入口

在“不提报商品”抽屉的商品列表中点击“一键提报”按钮。

### 6.2 提报范围

- 不展示批量选择框，不需要用户逐条勾选。
- 点击“一键提报”后，对当前商品列表中的全部数据发起手动提报。
- 若用户已输入商品ID或活动ID筛选条件，则对筛选后的商品列表数据发起手动提报。
- 当前商品列表无数据时，“一键提报”按钮置灰不可点击。
- 不提报商品列表中的所有数据类型均支持手动提报。
- 支持范围包括：
  - 按商品排除数据。
  - 按活动排除数据。
  - 按活动+商品排除数据。
- 一键提报范围不得仅限制为有商品 ID 的数据。

### 6.3 手动提报弹窗

弹窗标题：手动提报。

弹窗说明：

`将对商品列表中的 X 条不提报数据发起手动提报。列表中的按商品、按活动、按活动+商品排除数据均支持手动提报。`

弹窗字段：

- 服务费分成比例。
- 联系方式。

底部按钮：

- 取消。
- 确认。

### 6.4 服务费分成比例

服务费分成比例为必填项，支持两种配置方式：

#### 固定分成

选择“固定分成”时，仅展示固定比例选项：

- 1:9
- 2:8
- 3:7
- 4:6
- 5:5

默认选中：2:8。

#### 自定义

选择“自定义”时，仅展示自定义输入区：

- 团长自留输入框。
- 百分号。
- 向外提报比例。

交互规则：

- 团长自留输入范围为 0 到 100。
- 向外提报比例 = 100 - 团长自留。
- 示例：团长自留 20%，向外提报 80%。

### 6.5 联系方式

- 联系方式为必填。
- 默认展示示例手机号：18618267721。
- 联系方式为空时，“确认”按钮不可用。

### 6.6 确认提报

点击“确认”后：

- 关闭手动提报弹窗。
- 生成一条手动提报记录。
- 刷新手动提报记录数据。
- 吐司提示：`手动提报申请已发起，您可在【手动提报记录】中查看详情`

吐司样式：

- 纯文字。
- 字号 14px。
- 不加粗。
- 不展示图标。

## 7. 手动提报记录

### 7.1 入口

点击“不提报商品”抽屉商品列表操作区的“查看手动提报记录”文字链接，打开手动提报记录抽屉。

入口样式：

- 纯蓝色文字。
- 无边框。
- 无背景。

### 7.2 抽屉说明

说明文案：

`展示从不提报商品列表发起的手动提报记录，仅保留近三个月的记录`

### 7.3 表格字段

| 字段 | 说明 |
| --- | --- |
| 提报时间 | 手动提报申请发起时间 |
| 提报总数 | 本次提报申请的总计划数 |
| 提报成功数 | 本次提报申请成功的总计划数 |
| 提报失败数 | 本次提报申请失败的总计划数 |
| 提报服务费分成比例 | 本次提报使用的服务费分成比例 |
| 任务状态 | 当前手动提报任务状态 |

### 7.4 字段说明浮层

以下字段标题旁展示说明图标，hover 后展示说明：

| 字段 | 说明文案 |
| --- | --- |
| 提报总数 | 本次提报申请的总计划数（包含提报申请成功数和提报申请失败数） |
| 提报成功数 | 本次提报申请成功的总计划数 |
| 提报失败数 | 本次提报申请失败的总计划数 |

### 7.5 展示规则

- 提报总数、提报成功数、提报失败数仅展示数字，不追加“条”。
- 无记录时展示缺省态：暂无手动提报记录。
- 记录仅保留近三个月。

### 7.6 任务状态逻辑

任务状态包含：

| 状态 | 说明 |
| --- | --- |
| 待处理 | 已发起手动提报，提报任务还未进行处理 |
| 处理中 | 正在处理中，正在陆续执行提报任务 |
| 处理完成 | 所有可报名的都已经提报完成 |

## 8. 删除不提报数据

### 8.1 入口

在不提报商品列表中点击单条数据的“删除”按钮。

### 8.2 二次确认

点击删除后展示确认弹窗：

- 标题：确认删除。
- 说明：删除后，该商品将从不提报商品列表中移除。
- 按钮：取消、删除。

确认删除后：

- 从列表移除该条数据。
- 刷新列表和分页。

## 9. 空态与提示

### 9.1 不提报商品列表空态

- 无数据时展示：暂无内容。
- 有数据时不展示空态。

### 9.2 手动提报记录空态

- 无记录时展示：暂无手动提报记录。

### 9.3 吐司提示

| 场景 | 文案 |
| --- | --- |
| 商品 ID 为空 | 商品ID不能为空! |
| 商品 ID 格式错误 | 商品ID输入错误! |
| 活动 ID 为空 | 活动ID不能为空! |
| 手动提报确认成功 | 手动提报申请已发起，您可在【手动提报记录】中查看详情 |
